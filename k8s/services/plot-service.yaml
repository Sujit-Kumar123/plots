apiVersion: apps/v1
kind: Deployment
metadata:
  name: plot-service
  namespace: plots-system
  labels:
    app: plot-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: plot-service
  template:
    metadata:
      labels:
        app: plot-service
    spec:
      containers:
        - name: plot-service
          image: plots/plot-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8003
          envFrom:
            - configMapRef:
                name: plots-config
          env:
            - name: DATABASE_URL
              value: "postgresql+asyncpg://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@postgres:5432/$(POSTGRES_DB)"
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: plots-secrets
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: plots-secrets
                  key: POSTGRES_PASSWORD
            - name: INTERNAL_SERVICE_SECRET
              valueFrom:
                secretKeyRef:
                  name: plots-secrets
                  key: INTERNAL_SERVICE_SECRET
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 20
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8003
            initialDelaySeconds: 15
            periodSeconds: 5
            failureThreshold: 6
---
apiVersion: v1
kind: Service
metadata:
  name: plot-service
  namespace: plots-system
spec:
  selector:
    app: plot-service
  ports:
    - port: 8003
      targetPort: 8003
  type: ClusterIP
