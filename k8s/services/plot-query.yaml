apiVersion: apps/v1
kind: Deployment
metadata:
  name: plot-query
  namespace: plots-system
  labels:
    app: plot-query
spec:
  replicas: 3
  selector:
    matchLabels:
      app: plot-query
  template:
    metadata:
      labels:
        app: plot-query
    spec:
      containers:
        - name: plot-query
          image: plots/plot-query:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8004
          envFrom:
            - configMapRef:
                name: plots-config
          env:
            - name: DATABASE_URL
              value: "postgresql+asyncpg://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@postgres:5432/$(POSTGRES_DB)"
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: plots-secrets
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: plots-secrets
                  key: POSTGRES_PASSWORD
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 15
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: plot-query
  namespace: plots-system
spec:
  selector:
    app: plot-query
  ports:
    - port: 8004
      targetPort: 8004
  type: ClusterIP
